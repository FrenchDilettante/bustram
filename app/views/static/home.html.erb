<div ng-controller="SearchController">
  <div class="form-group">
    <%= label_tag :q, 'Chercher un arrêt' %>
    <input type="text"
           name="q"
           id="q"
           class="form-control"
           autocomplete="off"
           autofocus
           ng-model="q"
           ng-model-options="{ debounce: {'default': 200, 'blur': 0} }" />
  </div>

  <ul class="stop-list">
    <li ng-repeat="stop in stops">
      <a ng-href="/arrets/{{ stop.id }}">
        {{ stop.name }}
      </a>
    </li>
  </ul>
</div>
